# Starter

Extend `schema-skeleton.sql`, load `seed-sample.csv` into a scratch table if it
helps local testing, and produce the deliverables listed in the brief. The
skeleton is intentionally incomplete.
