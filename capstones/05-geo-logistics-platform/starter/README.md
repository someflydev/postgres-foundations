# Starter

Load the `logistics_geo` domain pack, then replace `schema-skeleton.sql` with a full design. Keep dispatch scoring outside the database and focus on durable operational data, spatial access, partitioning, and explainable queries.
