# Starter

Extend `schema-skeleton.sql` into a complete scheduling design. Use the sample
CSV only as lightweight test data; the real deliverable is the schema,
queries, concurrency scenario, runbook, and writeup.
