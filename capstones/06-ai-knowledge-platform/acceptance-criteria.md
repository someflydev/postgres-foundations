# Acceptance Criteria

- Schema stores documents, document chunks, embeddings, and access scope.
- Index artifact includes GIN FTS, GIN trigram, and HNSW vector indexes.
- Critical queries cover lexical, fuzzy, semantic, and hybrid retrieval.
- Writeup compares pgvector with FTS and pg_trgm before justifying semantic retrieval.
