# Constraints

- Use postgres_fdw deeply, including pushdown and async append discussion.
- Use pg_stat_statements on both new and legacy databases.
- Configure logical replication posture for the BI replica.
- Use FTS and pg_trgm for search.
- Make an explicit Citus decision with distribution-key reasoning.
