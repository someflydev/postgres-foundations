# PostgreSQL Config Overlays

Later prompts will add focused PostgreSQL configuration fragments here for
modules such as partitioning, logical replication, and pg_stat_statements
observability. The Compose file does not mount this directory yet; that mount
will be added when those phases need override configuration.
