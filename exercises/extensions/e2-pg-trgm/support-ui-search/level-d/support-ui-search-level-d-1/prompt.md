# Support UI Search Level D1

Diagnose a present GIN trigram index that is not used because the query is LIKE 'prefix%' and a btree expression index would serve it better.

Return the SQL result or written triage note requested by the exercise metadata.
