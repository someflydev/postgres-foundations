One acceptable critique for pgbouncer-pool-modes-revisited-level-d-1.
Transaction-pooling breaks session-scoped LISTEN/NOTIFY; diagnose the failure and propose alternatives such as moving LISTEN connections to a non-pooled path.
A strong answer identifies the failing assumption, the operational blast radius, and a safer PostgreSQL-first alternative.
