# Integrating with EXPLAIN Workflows Level D1

Diagnose a pg_stat_statements integration that misses the worst queries because pg_stat_statements.max is too low and entries cycle out of the stats table.

Return the SQL result or written triage note requested by the exercise metadata.
