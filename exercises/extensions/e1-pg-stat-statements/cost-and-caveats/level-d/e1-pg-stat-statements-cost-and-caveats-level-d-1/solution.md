A strong answer explains that pg_stat_statements.max is too low, so normalized statement entries churn before weekly triage can see the worst queries. Increase the setting, restart as required, reset at the beginning of a clean baseline, and compare total_exec_time, calls, mean_exec_time, and I/O counters after the next representative window. Do not treat an empty top-N as proof that the workload is healthy.
