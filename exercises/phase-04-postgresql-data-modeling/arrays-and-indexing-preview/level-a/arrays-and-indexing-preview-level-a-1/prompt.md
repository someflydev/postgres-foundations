# Recognize the good fit: Arrays and Indexing Preview

## Setup

Use the PostgreSQL Foundations lab and the `ecommerce` seed pack at phase 4b.

## Given

Use the lesson topic: recognizing that tags @> has an index shape without building it yet.

## Task

Identify why noticing the future GIN index signal after the model is right is a good fit and name one bad-fit alternative.

## Success Criteria

- Name the modeled fact before the PostgreSQL feature.
- Include both a good-fit and bad-fit example when prose is requested.
- Stay inside the Phase 4b boundary.

## Estimated Time

See `exercise.json`.
