# Use the Phase 4b operator: Ranges vs Arrays vs Child Tables

## Setup

Use the PostgreSQL Foundations lab and the `ecommerce` seed pack at phase 4b.

## Given

Use the lesson topic: deciding between tags, slots, multiranges, and child rows.

## Task

Use the lesson's PostgreSQL operator or function on deciding between tags, slots, multiranges, and child rows.

## Success Criteria

- Name the modeled fact before the PostgreSQL feature.
- Include both a good-fit and bad-fit example when prose is requested.
- Stay inside the Phase 4b boundary.

## Estimated Time

See `exercise.json`.
