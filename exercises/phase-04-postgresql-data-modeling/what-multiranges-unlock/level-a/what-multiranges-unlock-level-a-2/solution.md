# Reference Solution

A strong answer explains that a calendar with multiple separated available windows is the good-fit shape, while inventing comma-delimited times or scattered boolean flags is the failure mode. It names the migration or query that makes PostgreSQL carry the truth directly, and it keeps indexing details as a later GIN or GiST pointer rather than the first fix.
