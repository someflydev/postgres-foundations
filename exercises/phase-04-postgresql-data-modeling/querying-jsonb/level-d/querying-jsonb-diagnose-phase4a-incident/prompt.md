# Diagnose and repair the incident: Querying JSONB

## Setup

Use the PostgreSQL Foundations lab and the `ecommerce` seed pack at phase 4a.

## Given

Use the Phase 4a seed data for `ecommerce`. The lesson topic is JSONB querying.

## Task

A JSONB containment query is slow and someone proposes the wrong index. Explain why it is slow, why a GIN index would be a later fix, and do not implement that index in Phase 4a.

## Success Criteria

- The answer names the modeled fact.
- The answer includes both a good-fit and bad-fit example when prose is requested.
- The answer stays inside the Phase 4a boundary.

## Estimated Time

45 minutes.
