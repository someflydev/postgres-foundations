# Reference Solution

A strong answer explains that product tags that are small, bounded, and owned by the product row is the good-fit shape, while roles, permissions, line items, or prices that need metadata and history is the failure mode. It names the migration or query that makes PostgreSQL carry the truth directly, and it keeps indexing details as a later GIN or GiST pointer rather than the first fix.
