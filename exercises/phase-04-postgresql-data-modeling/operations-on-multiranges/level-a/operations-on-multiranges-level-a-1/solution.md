# Reference Solution

A strong answer explains that using PostgreSQL operators to derive practical availability is the good-fit shape, while splitting all calendar arithmetic into application loops is the failure mode. It names the migration or query that makes PostgreSQL carry the truth directly, and it keeps indexing details as a later GIN or GiST pointer rather than the first fix.
