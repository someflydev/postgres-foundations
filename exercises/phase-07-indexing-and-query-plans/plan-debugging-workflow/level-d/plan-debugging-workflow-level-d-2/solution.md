The severe estimated-vs-actual mismatch should be handled by running ANALYZE first, then considering CREATE STATISTICS on correlated columns or rewriting the predicate so the planner can see the relationship. Do not add another index until the row estimate has been explained. Record before and after EXPLAIN ANALYZE BUFFERS output and keep the change that reduces buffers without hiding the real distribution problem.
