# Write Amplification and Bloat Level B1

Run `pgfound lab explain starter.sql`. Record the scan node, estimated rows, actual rows, execution time, shared hits, and shared reads. Then write two sentences explaining whether the plan is good enough before adding any index. This is a row and buffer comparison drill for write amplification, redundant indexes, and bloat.
