# Single-column Indexes and Selectivity Level A2

Run `pgfound lab explain` for `starter.sql`, or inspect the supplied `EXPLAIN ANALYZE` shape if the lab is unavailable. Identify the access path PostgreSQL picked, the relation it read, and why that path fits the query pattern. This is a plan identification drill for single-column selectivity.
