# Solution

The reference solution in `solution.sql` demonstrates one valid answer for When a Materialized View Helps. Equivalent answers are acceptable when they preserve row grain, filtering semantics, deterministic ordering where required, and the same output columns.
