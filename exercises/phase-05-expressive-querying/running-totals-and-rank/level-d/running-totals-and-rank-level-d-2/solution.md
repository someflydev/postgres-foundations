# Solution

The reference solution in `solution.sql` demonstrates one valid answer for Running Totals and Rank. Equivalent answers are acceptable when they preserve row grain, filtering semantics, deterministic ordering where required, and the same output columns.
